<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 1. 어노테이션 기반 컨트롤러 활성화 -->
    <mvc:annotation-driven />

    <!-- 2. 업로드 파일 리소스 허용 (프로젝트 내부 경로 매핑) -->
    <mvc:resources mapping="/upload/**" location="/upload/" />

    <!-- 3. 정적 리소스 매핑 -->
    <mvc:resources mapping="/resources/**" location="/resources/" />

    <!-- 4. ViewResolver 설정 (JSP 파일 위치 정의) -->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!-- 5. 컴포넌트 스캔 (Controller, Service, Repository 포함) -->
    <context:component-scan base-package="com.itwill">
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Service"/>
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Repository"/>
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Component"/>
    </context:component-scan>

    <!-- 6. 파일 업로드 처리용 multipartResolver 설정 -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="defaultEncoding" value="UTF-8" />
        <property name="maxUploadSize" value="104857600" /> <!-- 최대 업로드 파일 크기: 100MB -->
    </bean>

    <!-- 7. 이미지 파일 매핑 -->
    <mvc:resources mapping="/image/**" location="/image/" />

</beans>
